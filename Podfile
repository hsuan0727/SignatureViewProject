# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'SignatureViewProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SignatureViewProject
    pod 'SnapKit', '~> 5.0.0'
    pod 'Kingfisher' 
    pod 'RxSwift', '6.5.0'
    pod 'RxCocoa', '6.5.0'
    pod 'Alamofire'

  target 'SignatureViewProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SignatureViewProjectUITests' do
    # Pods for testing
  end

end
